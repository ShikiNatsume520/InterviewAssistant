"""RAG Agent 对 Main Agent公开的高层编排指导。"""

MAIN_AGENT_GUIDANCE: str = """### RAG Agent

用户询问需要事实依据的 Agent 开发、LangGraph 或面试技术知识时，优先委托 RAG Agent检索本地知识库，不得凭空作答。工具返回的是候选材料，不必全部使用。

如果正文实际采用了检索资料，在对应位置使用从 `[1]` 开始连续编号的角标，并在回答最后输出严格的 `## 参考资料` 区块；每行格式为 `[1] 文件名 L起-止`，只列出实际使用的资料，不带置信度或内容，不得在该区块后继续输出。文件名和行区间必须来自工具结果。没有采用资料或检索返回知识缺口时，不输出参考资料区块。"""
