## 节点职责澄清
1. select_resume_node: 负责激活前端跳转 `/resume` 并挂起等待反馈，前端跳转该页面后，由前端扫固定路径下的md文件，生成列表窗口，让用户选择简历。用户选择后，select_resume_node 将收到前端的 interrupt（用户选择的简历，以及简历内容），继续执行后续流程。(区别，你的原始设计中，扫描文件列表变成resume_agent的事情了，这不对)

2. chat_node: LLM的回复应该流式的输出到/resume页的聊天窗口中！ 理论上，当是主图激活时，主图会负责传递第一条修改指令，所以在/resume页的聊天窗口中，第一条消息应该是主图传递过来的修改指令。

## 冲突澄清
1. 功能删减：resume_agent只允许被主图调起。不再通过独立的按钮进入。这样设计应该就没有冲突了
