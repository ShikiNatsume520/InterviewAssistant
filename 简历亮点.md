## Phase 1 — Index Agent 与本地知识库基础设施

### 遇到的困难

1. **DeepSeek thinking 模型结构化输出限制**：index.md 的 LLM 增量维护需要结构化输出 `IndexUpdate`，但 deepseek-v4-flash 等 thinking 模型不支持强制 `tool_choice=required`（`with_structured_output` 会返回 400）。解决方案：`bind_tools([IndexUpdate])` 使用 `auto` 策略 + 后端取 `tool_calls` 校验 + 未调用时从返回文本兜底抽取 JSON。

2. **Markdown 切片的边界处理**：需要处理 CRLF/LF 归一化、代码围栏（`````/~~~~`）内 `#` 不当标题、超长块的字符级二次切分保留 50 字符 overlap，同时保持行号 1-based 闭区间的精确性。

3. **本地 embedding 模型加载**：`SentenceTransformer` 加载 `bge-small-zh-v1.5`（~95MB）首次下载慢，且需确保模型路径在开发机和 CI 上均可发现。用模块级单例缓存避免重复加载。

### 实现的功能

- **五节点 Index Agent 子图**（`src/index_agent/`，`scan→chunk→embed→llm_index→write_index`），不进主图、不进 `AgentRegistry`，作为独立的后台 agent 注册到 `langgraph.json`
- **行级 markdown 切片**：按标题结构分割，metadata 严格保留 `start_line`/`end_line`；代码围栏内 `#` 不误切；超长块以字符级二次切分 + 50 字符 overlap 防止关键信息被截断
- **Chroma 向量灌入**（单 collection `knowledge_base`）：切片向量化后 upsert，metadata 携带文件名/行号/标题，支持按 `chunk_id` 去重覆盖
- **LLM 维护的语义索引 `data/index.md`**：LLM 看"现有索引 + 新文件 chunks"后判断合并/新建索引行，输出 Markdown 表格（关键词/摘要/文件引用）。引用格式由后端拼接，LLM 不碰格式细节
- **本地 embedding 模型集成**（`bge-small-zh-v1.5`，sentence-transformers，GPU 自动启用），模块级单例缓存避免重复加载
- **`langgraph.json` 注册 + SDK 测试脚本**：可通过 `langgraph dev` 启动 Studio 可视化调试

### 设计亮点

#### 1. 双轨索引架构：向量召回 + LLM 语义索引互补

没有用单一的"向量库一把梭"，而是构建了两条物理隔离、语义互补的索引轨道：
- **行级向量索引**（Chroma）：做"片段级"稠密召回，metadata 全程携带 `start_line/end_line`；
- **LLM 维护的语义索引**（`index.md`）：做"主题级"导航——以 关键词/摘要/文件 维度组织。

两者粒度不同、用途不同，为后续混合检索打下物理基础。这一设计本质是**承认稠密检索与稀疏语义导航各有盲区，用双轨互补**，而非迷信单一召回路径。

#### 2. 后台 Agent 与对话主图的彻底解耦

Index Agent 被定位为**数据导入触发的后台维护 agent**，明确不进主图流程、不进 `AgentRegistry`、不被对话节点调用。主图只**只读**其产出（`index.md` / Chroma）。

这是把"**索引重建（写路径，慢、可重试）**"与"**问答检索（读路径，需快）**"做了读写分离与关注点分离——避免了把数据维护这种副作用塞进实时对话回路所带来的耦合与延迟。

#### 3. LLM 承担"主题归类与去重合并"的增量索引维护

`index.md` 不是全量重建，而是 LLM 看到"**现有索引 + 新文件 chunks**"后自主判断**合并已有项 / 新建条目**，输出更新后的完整表。

把"哪些新内容该并入已有主题、哪些值得独立成条"这种**难以用规则表达的语义判断**交给 LLM，而把确定性的格式化留给代码——这是 **LLM-as-judge 思想在数据管道而非对话场景**的落地。

#### 4. 行级引用作为贯穿全链路的一等约束

`start_line/end_line` 不是检索时才补的元数据，而是从**切片→向量化→检索返回**全链路的一等公民。即便是超长块的二次字符切分（带 overlap），行号区间也严格对应到原文件的真实行。

这让"**带 `[文件名](start~end)` 的可溯源引用**"成为系统能力而非事后拼接，直接支撑了 RAG 的可信度——这是把一条业务硬约束贯彻到数据模型的体现。

#### 5. 引用格式职责分离 + 工具调用三级降级（鲁棒性设计）

两个"把易错责任移出 LLM"的细节：
- **职责分离**：LLM 只产语义内容（keywords/summary/files），引用格式 `[文件名](path)` 完全由后端确定性代码拼接。LLM 不碰格式、不碰行号。
- **工具调用降级**：针对 thinking 模型无法强制 `tool_choice` 的限制，采用 `bind_tools(auto)` → 取 `tool_calls` 校验 → 未调用则兜底抽 JSON 的三级策略，保证结构化输出在异构模型后端下的可用性。

核心思想一致：**凡是能用确定性代码兜住的，就不把正确性押在 LLM 上**。

---

## Phase 2 — 混合 RAG 子图

### 遇到的困难

1. **Embedding 判别力不足**：本地 `bge-small-zh-v1.5`（512 维）在短文本块（<50 字符）上对"无关查询"（如"量子计算原理"对 LangGraph 知识库）也产出了 0.648 的高分，导致 gap 检测形同虚设。**解决**：切到硅基流动 `BAAI/bge-large-zh-v1.5`（1024 维，在线 API），判别力显著提升。

2. **Gap 判定从"分数阈值"到"分差比"的演进**：初始用 L2 距离的绝对阈值（0.25），原型验证发现无关查询仍然 ≥0.6 → 切换到 Cosine 空间后改善了分数分离度但绝对阈值仍不可靠 → 最终采用 **`top_score / avg(all_scores)` 分差比（阈值 1.15）**，区分"确定的检索"与"没把握的噪声"。

3. **Chromba EmbeddingFunction 协议适配**：Chroma 1.5.9 对自定义 EmbeddingFunction 签名检测严格——必须实现 `__call__(self, input)`（参数名必须叫 `input`）+ `name()` + `embed_query(input)` + `embed_documents(input)`——逐一调试才通过。

### 实现的功能

- **双管道 RAG 子图**（`src/rag_agent/`，两节点 `retrieve→aggregate`）
  - **semantic 管道**：余弦向量召回 → 标题节扩展（相邻/重叠行区间合并）→ 分差比判置信度 → 低置信时兜底全目录 grep
  - **keyword 管道**：`index.md` 关键词匹配定位候选文件 → 定向 grep 精确命中行 → 无候选时退化为全目录 grep
- **硅基流动在线 embedding 适配层**（`SiliconFlowEmbeddingFunction`，OpenAI 兼容协议），40 行代码完成 Chroma EmbeddingFunction 全协议实现
- **Chromba 余弦空间切换** + 向量库重建
- **langgraph.json 注册 + SDK 测试**（三场景验证：语义检索、关键词检索、gap 缺口）

### 设计亮点

#### 1. 分差比判据：用"确定性程度"而非"分数绝对值"判定知识缺口

传统 RAG 系统靠 `score > threshold` 判定"够不够相关"。但短文本块上分数分布压缩，绝对阈值不可靠。Phase 2 的 key insight 是：**当检索有把握时，top 结果会明显高于其余结果（分差约 20%）；当检索是噪声，所有结果分数簇拥在一起。** 用 `top_score / avg(all)` 衡量的不是"分数多高"，而是**"模型有多确定"**——这是从"打分思维"到"置信度思维"的切换。

特别适用于**小知识库**场景：全库只 7 个 chunk 时，向量检索永远返回 7 条，命中和噪声的表观差异只能从"谁更尖"而非"谁绝对高"判断。

#### 2. 双管道分离设计：语义覆盖 × 精确打击的分工协作

没有把 vector、index、grep 堆在一个大而全的检索器里，而是拆成两条**互斥路由的管道**——`semantic` 做语言相似的模糊覆盖（"加一加"/add_messages），`keyword` 做词汇精确的定向打击（"reducer"）。主图 Router（Phase 3）按场景选路，而非让 RAG 子图自己做权重融合。

这种**"路由在前、检索在后"**而非"混合检索再重排"的模式，本质是认同：不同的信息需求需要不同的检索策略，没有银弹式的融合方案。每条管道保持简单、可独立调优。

#### 3. 在线 Embedding API 替代本地模型：零部署代价的质量跃升

将 embedding 从本地 `sentence-transformers`（离线加载 95MB 模型）改为**硅基流动 `BAAI/bge-large-zh-v1.5`（OpenAI 兼容 API）**，维度从 512 翻倍到 1024。改动仅在一个文件中新增了 ~40 行的适配类，上下游代码无感知。

收益：① 每次查询的 embedding 计算从本地推理转为 API 调用（~200ms），但省去了 GPU 占用和模型文件维护；② 未来可随硅基流动升级模型，改一行 `.env` 即可。这是**将模型服务化、基础设施化**的思路——不把 embedding 模型作为"本地资产"而是"外部能力"，降低维护栈。

#### 4. 检索管道与索引管道的读写分离

Phase 1 的 `index_agent`（写路径：切片→向量化→灌库更新 `index.md`）和 Phase 2 的 `rag_agent`（读路径：检索→gap 判定）共享同一 Chroma collection 和配置文件，但**互不引用**（rag_agent 只 import `get_embed_fn()` 作为共享基础设施，不沾 index_agent 的图逻辑）。

这是**数据管道与消费管道的分离**：索引维护是"导入侧"的批量作业，检索是"查询侧"的实时交互。两者各自独立演进、单独调试、不需要在同一图中编排。

---

## Phase 3 — 动态主图总线

### 遇到的困难

1. **路由架构从 `AgentAction` + `Send` 演进到 `as_tool` + 拦截路由**：初始设计用 `AgentAction` 结构化输出 + `sub_agent_fanout_node` + 子图输入输出槽位 + `input_transform`/`output_transform` 变换函数，复杂度攀升。重构为 `bind_tools` + `route_after_chat` 拦截后，移除了所有槽位和变换函数，代码量下降约 40%。

2. **LangGraph 类型桩与 mypy --strict 的兼容**：`StateGraph.add_node`、`ToolNode`、`CompiledStateGraph.invoke` 等 API 在 LangGraph 类型桩中泛型参数复杂，mypy --strict 下产生大量误报。需用 `type: ignore` + `Any` 精确管理类型逃逸区域，在严格检查与三方库类型桩缺陷之间取得平衡。

3. **system prompt 与 tool schema 的职责边界**：初期 system prompt 中复述工具列表和参数说明，与 `bind_tools` 自带的 tool schema 冗余。最终确定 `@tool` 的 docstring 和 `Field` 描述承载所有工具语义，system prompt 只保留角色定位和行为准则。

### 实现的功能

- **`AgentRegistry` 动态注册子智能体**：`SubAgentMetaData` 管理子图；`register()` / `get_all_tools()` / `is_sub_agent()` 提供注册、工具聚合、路由判断能力
- **`chat_node` 全 LLM 原生决策**：`bind_tools(all_tools)` + `ChatPromptTemplate` 注入 system prompt；system prompt 写入 state.system_prompt 供 LangGraph Studio 调试面板直接查看
- **`route_after_chat` 子图拦截路由**：检查最后消息的 `tool_calls[0].name` → 子图工具走 wrapper 节点，普通工具走 `ToolNode`，无调用则 END
- **`rag_agent_node` 包装节点**：解析 `tool_call.args` → invoke `rag_subgraph` → 返回 `ToolMessage`（带引用内容）+ `citations`（结构化引用列表）
- **`MainState` 极简化**：只保留 `messages`（`add_messages`）+ `citations` + `system_prompt`，不预留任何子图 I/O 槽位
- **`src/client.py` LLM 集中初始化**：`get_chat_model()` 统一管理 API Key 和 `bind_tools`，被 graph.py 和 index_agent 共用
- **`prototypes/phase3_sdk_test.py` SDK 测试**：三场景（寒暄/semantic 检索/keyword 检索）共用同一 thread 模拟连续对话
- **`rag_agent._extract_terms` 简化为空白符切分**：约定 keyword 模式下由主图 agent 传入空格分隔的关键词列表，`query.split()` 即可，移除复杂正则

### 设计亮点

#### 1. 子图即工具：用 LLM 原生 tool calling 替代自定义路由协议

将子图通过 `@tool` 包装为标准 `Tool`，与普通工具统一 `bind_tools`。路由函数只需检查 `tool_calls[0].name` 即可判断去向，不再需要自定义 `AgentAction` 结构化输出 + 扇出节点 + `Send`。

这本质是把"Agent 路由"从**自定义决策协议**降级为**复用 LLM 的通用能力**——由 LLM（而非路由代码）决定"该调哪个工具/子图"。路由代码只做机械的"拦截与分发"，不做语义判断。减少了一套需要维护的决策协议。

#### 2. 路由拦截模式：子图分流与 ToolNode 隔离

router 在 `chat_node` 之后检查 `tool_call` 归属——子图工具走到 wrapper 节点，普通工具走到 `ToolNode`。子图**不注册到 `ToolNode` 中**，避免了 `ToolNode` 的自动执行逻辑与子图自定义状态映射之间的冲突。

这种"先分流、后执行"的模式保持了 ReAct 循环的完整性，同时让子图可以访问完整的图状态而非仅限于 `tool_call` 返回的字符串——这是工具调用（ToolNode）和子图调用（wrapper）的本质区别。

#### 3. 状态极简化：从"预留 I/O 槽"到"消息即协议"

从 v1 设计的 `rag_subgraph_input`/`rag_subgraph_output` 每子图 I/O 槽位 + `input_transform`/`output_transform` 变换函数，简化为 `messages` + `citations`。子图的输入经 `tool_call.args` 传递，输出经 `ToolMessage` + 额外 state 字段返回。

这是**消息总线思想**的实践——不引入领域特定的状态结构，让通用的 Message 机制承担所有子图间通信。新增子智能体不再需要定义新的槽位和变换函数，只需注册 tool + wrapper 节点即可。

#### 4. 多文件模块化：state / registry / tools / graph 职责分离

```
src/agent/
  state.py     — 仅定义状态结构（30 行）
  registry.py  — 子智能体注册与路由查询（70 行）
  tools/       — 每个子图独立文件，含 @tool 定义 + wrapper 节点
  graph.py     — 图拓扑编排 + 路由 + LLM 调用（90 行）
```

每份文件 30-90 行，各自可独立阅读和修改。新增子智能体只需向 registry 注册 + `tools/` 下加文件，不改动 `graph.py` 的核心拓扑。这是**注册-执行分离**：图拓扑固定（chat_node → router → wrapper/END），新增行为不走拓扑变更，走注册扩展。

---

## Phase 4 — 本地 SQLite 断点 + 长期记忆

### 遇到的困难

1. **子图 checkpointer 适配器兼容**：`rag_agent_node` 从 config 获取主图 `__pregel_checkpointer`（`_CustomCheckpointerAdapter`）后编译子图，但同步 `invoke` 触发 `SyncPregelLoop` 调用的 `get_tuple()` 在该适配器上未实现 → `NotImplementedError`。最终方案：`rag_agent_node` 改为异步节点，子图沿用同一 checkpointer，用 `ainvoke` 调用→ `AsyncPregelLoop` → `aget_tuple()` 正常工作。核心教训：**platform 的 checkpointer 适配器只实现了异步接口，子图的调用方式必须与之匹配。**

2. **AgentRegistry 运行时查找导致 Studio 黑盒**：包装节点通过 `get_registry().get_sub_graph("rag_agent")` 运行时查找子图，Studio 编译期无法静态追踪函数体内实际调用了哪个子图，节点在拓扑图中显示为黑盒函数。**彻底移除整个 AgentRegistry 组件**，改为模块级直接导入 `build_rag_workflow` + 节点硬编码注册，Studio 可递归展开子图内部（`retrieve` / `aggregate` 节点可见）。

3. **LangGraph Server 自定义 checkpointer 冲突**：模块级 `graph = build_main_graph()` 内嵌了 `SqliteSaver`，但 `langgraph dev` 平台自动管理持久化，禁止图中携带自定义 checkpointer → 启动报错。修复：模块级 `graph` 编译时传 `checkpointer=False` 让平台接管。

4. **SqliteStore 事务管理**：`SqliteStore` 内部自行管理事务，但 `sqlite3.connect()` 默认启用自动事务 → `put()` 时报 "cannot start a transaction within a transaction"。修正：连接时加 `isolation_level=None` 关闭自动事务管理，交由 SqliteStore 内部控制。

### 实现的功能

- **SqliteSaver checkpointer 持久化**：主图绑定 `SqliteSaver`（`sqlite_checkpoints.db`），崩溃后 `get_state_history()` 可回溯线程状态并继续执行
- **Memory 子图**（`src/agent/memory.py`）：
  - `save_memory_node`：过滤 User/AI 消息 → LLM 提取结构化事实 → `Store.put()` 去重写入
  - `load_memory_context`：从 Store 读取用户画像 → 拼接到 `chat_node` 的 system_prompt 尾部 `## Memory` 板块
  - 进程内 `_memory_cache` 避免同一对话轮次中多次查 Store，写入新事实后自动清除
- **`SqliteStore` 持久化长期记忆**：用户画像存盘（`sqlite_store.db`），跨进程重启不丢失
- **异步子图节点**：`rag_agent_node` 从 config 获取主图 checkpointer 惰性编译子图，`async def` + `ainvoke` 兼容 `_CustomCheckpointerAdapter` 的异步接口
- **全局 Registry 移除**：`AgentRegistry`/`SubAgentMetaData` 全部删除，路由函数硬编码工具名判断，`build_main_graph` 静态注册所有节点
- **两种测试脚本**：
  - `phase4_direct_test.py`：AsyncSqliteSaver 本地持久化，独立运行直接图测试
  - `phase4_sdk_test.py`：基于 LangGraph SDK，需 `langgraph dev` 服务，可被 Studio 捕获
- **`src/agent/persistence.py`** 新增：`get_checkpointer()` / `get_store()` 工厂函数
- **`prototypes/`** 保留多版本调试原型

### 设计亮点

#### 1. 子图 checkpointer 继承：从主图 config 惰性编译 + 异步适配

RAG 子图**不是无状态纯计算单元**——它通过在 `rag_agent_node` 首次调用时从 `config.configurable.__pregel_checkpointer` 获取主图的 checkpointer，以相同实例编译子图，实现 checkpointer **自上而下的链式继承**。

```
rag_agent_node 首次调用:
  __pregel_checkpointer = _CustomCheckpointerAdapter(platform 异步适配器)
  → build_rag_workflow().compile(checkpointer=cp)  ← 同一实例
  → 后续调用复用缓存

子图 invoke:
  await rag_graph.ainvoke(input, config)  ← 异步路径
  → AsyncPregelLoop → aget_tuple()  ← 适配器实现了异步接口
```

这意味着：如果子图内部节点在复杂计算中途崩溃，其执行状态可从 checkpoint 恢复，无需重跑。对于当前 `retrieve → aggregate` 两节点子图来说收益不明显，但为后续更复杂的子图（如 Deep Research 多轮 Web 爬取）保留了恢复能力——**子图内部的每一步执行状态都在 SQLite 中有记录，与主图共享同一个持久化命名空间**。

选择异步 `ainvoke` 而非同步 `invoke`，是因为 platform 的 `_CustomCheckpointerAdapter` 只实现了 `aget_tuple`（异步）而未实现 `get_tuple`（同步）。这是**运行时接口检测驱动架构决策**：让子图调用方（`rag_agent_node`）去适配 checkpointer 的接口形态，而非让 checkpointer 去实现全套同步方法。`async def` 节点在同步主图中由 LangGraph 自动调度，无需上层调用方感知。

#### 2. 记忆缓存与存储分离：进程缓存 × SqliteStore 持久化

```
load:  _memory_cache(命中) → 返回
       _memory_cache(未命中) → SqliteStore.search() → 填充缓存 → 返回

write: LLM 提取 → SqliteStore.put() → 清除缓存
```

缓存保证同一轮对话中多次 `chat_node` 循环不重复读盘；`save_memory_node` 写入后自动清除缓存，下次加载读到最新数据。这是**读优化（缓存）与写一致性（清除）**的经典缓存模式在 Agent 记忆场景中的落地。

#### 3. 从动态注册到静态拓扑：移除整个编排层的工程取舍

Phase 3 设计了 `AgentRegistry` + `SubAgentMetaData` 来实现"可拔插子智能体注册"。Phase 4 实践中发现：① 运行时注册导致 Studio 无法静态追踪子图；② 全局 `set_registry()` / `get_registry()` 增加了隐式状态依赖；③ 实际只有 1 个子图，注册层的抽象收益无法覆盖其复杂度。

最终决定：**移除整个 registry.py**（按需引入的通用框架 → 硬编码）。`build_main_graph` 静态注册全部节点，`route_after_chat` 硬编码 `if tool_name == "rag_agent"`。新增子图时修改位置明确（共 4 处），虽不及注册层"自动化"，但每个动作都在同一个文件中、编译期可见、无隐式依赖。

这是**延迟抽象**的实践——不要为"可能需要的扩展"提前构建抽象层，等到扩展真正发生时再做。在此之前，重复比间接更容易理解。

#### 4. 异步适配层：同步图 + 异步子图节点的共存

`rag_agent_node` 是 `async def`，其余节点是同步函数。LangGraph 支持同一图中混合 async/sync 节点，运行时自动调度。这让 `rag_agent_node` 可以使用 `ainvoke` 调用子图，兼容 platform 的异步 checkpointer，而其他节点继续保持同步、不受影响。

这是**局部异步化**——不将整个图改为全异步（会增加所有节点的回调复杂度），而是让需要异步 I/O 的节点单独 async，其余保持同步。适用场景：子图调用、外部 API 调用、文件 I/O 等"局部阻塞且可能有异步适配需求"的操作。

---

## Phase 5 — 简历优化子图 + FastAPI 端到端

### 遇到的困难

1. **interrupt 跨主图子图传播 + Command 恢复机制**：resume 子图内部用 `interrupt()` 挂起等用户确认，但子图是被主图 wrapper 节点 `ainvoke` 调用的。难点在于：子图的 `interrupt()` 能否让整个主图线程暂停？下一轮用户输入如何精准恢复到子图挂起点？原型验证发现：子图 `interrupt()` 会抛 `GraphInterrupt`，wrapper 节点**透传该异常**（`try: await rg.ainvoke(...) except GraphInterrupt: raise`）即可让主图 Pregel 循环捕获并暂停线程；下一轮 `Command(resume=value)` 经主图重新进入 wrapper，子图靠 checkpointer 从挂起点恢复继续而非重跑。

2. **InjectedState 工具写回 state 的返回值约束**：section 级 CRUD 工具需要读 `current_draft`、写回 `current_draft` + `last_draft` 快照。原型用 `Annotated[ResumeState, InjectedState]` 注入读草稿，但工具返回**裸 dict** 直接被 ToolNode 拒绝。读 LangGraph 1.2.6 源码发现 `ToolNode._normalize_tool_response` 严格要求返回值是 `Command | ToolMessage | list`——裸 dict 抛 `TypeError`。最终用 `ToolRuntime` 注入（含 `tool_call_id`/`state`）+ 返回 `Command(update={...})` 写回草稿字段 + 附带 ToolMessage。

3. **SSE 流式分帧的 CRLF 陷阱**：自建 FastAPI 前端用 `fetch` 读 SSE 流时，interrupt 弹窗始终不出现。后端日志确认 `pending_interrupt=True` 且 interrupt 事件已 yield，但前端 `总帧数=0`。根因是 sse-starlette 用 **CRLF（`\r\n`）** 而非 LF 做行分隔，帧分隔是 `\r\n\r\n`，前端 `indexOf("\n\n")` 永远匹配不到，所有 chunk 堆积在 buffer 直到流结束。改用 `\r\n\r\n` 分帧 + `/\r?\n/` 切行后修复。

4. **前端 interrupt 状态的跨请求残留**：前端用全局 `pendingInterrupt` 变量暂存 interrupt，但请求开始时未清理，导致新会话的 interrupt 事件被 `pendingInterrupt === null` 判断丢弃。改为请求内局部变量 `localInterrupt`，流结束才赋给全局，消除跨请求污染。

5. **Studio 子图展开为黑盒的架构权衡**：Studio 中子图节点显示为不可展开的黑盒。排查确认根因是 wrapper 函数内 `await rag_graph.ainvoke(...)` 模式——Studio 静态分析时只看到普通节点函数。验证了 LangGraph 官方"子图作为节点"模式（`StateGraph(state, input_schema=..., output_schema=...)` + `add_node(name, compiled_subgraph)`，LangGraph 自动做主图↔子图状态过滤）能让 Studio 展开，但需要额外加 tool_call 解包/打包转换节点，是较大的架构调整。经权衡**决定不实施**——保持 wrapper ainvoke 模式，用 web 前端 + 调试日志 + Studio state 查看替代子图可视化。

### 实现的功能

- **resume_agent 简历优化子图**（`src/resume_agent/`，plan/plan_confirm/react_router/resume_tools/step_confirm/finalize 六节点）：LLM 规划 → interrupt 等用户确认计划 → ReAct 循环调用 CRUD 工具 → 每步 interrupt 等用户批准/拒绝/建议 → 完成退出
- **section 级 CRUD 工具**（`add_section`/`update_section`/`delete_section`/`reorder_sections`/`show_draft`）：用 `ToolRuntime` 注入读草稿、`Command(update=...)` 写回草稿 + `last_draft` 快照
- **plan 软约束遵循**：`plan_steps` + `steps_completed` 进度标记注入 react_router 的 system prompt，LLM 自主按序执行
- **单步回退**：CRUD 工具写入前快照 `last_draft`，`step_confirm` 用户拒绝时恢复快照（不维护版本栈）
- **resume_agent 主图接入**（与 rag_agent 同构）：`@tool` 声明 + `route_after_chat` 拦截 + wrapper 节点惰性编译子图 + `GraphInterrupt` 透传
- **resume 子图内复用 rag_agent 单例**：`rag_agent_in_resume_node` 直接调用主图的 `rag_agent_node`，`_rag_graph` 缓存全局共享，进程内只编译一次
- **FastAPI `/v1/chat` SSE 端到端**（`src/agent/server.py`）：`AsyncSqliteSaver` + `SqliteStore` 持久化，token 级流式（`stream_mode="messages"`），interrupt 检测 + `Command(resume=...)` 恢复，三种 SSE 事件（token/interrupt/done）
- **web 调试前端**（`static/index.html`）：会话管理、token 流式渲染、interrupt 弹窗（计划确认/单步 diff 确认 + 批准/拒绝/建议按钮）、引用展示、草稿实时查看
- **统一调试日志**（`src/agent/debug.py`）：`dlog(scope, node, msg, **extra)` + `summarize_messages()`，覆盖主图/rag/resume/server 各节点，`IA_DEBUG=1` 开关
- **简历模板样例 + 索引重建**：`data/markdown/resume_template.md` 走 RAG，resume_agent 优化时可调 rag_agent 检索模板写法参考

### 设计亮点

#### 1. GraphInterrupt 透传：让 interrupt 子图与主图集成方式同构于普通子图

resume_agent 与 rag_agent 的**集成方式完全一致**（`@tool` + `route_after_chat` 拦截 + wrapper ainvoke），唯一差异是 wrapper 捕获并透传 `GraphInterrupt`。这是关键设计：**不为主图-子图 interrupt 通信引入新的协议或状态字段**，而是复用 LangGraph 原生的 interrupt 机制——子图 interrupt 抛异常 → wrapper 透传 → 主图 Pregel 捕获并暂停线程 → checkpoint 持久化 → 下一轮 `Command(resume=...)` 经主图重新进入 wrapper → 子图靠 checkpointer 从挂起点恢复。

这意味着 interrupt 子图的"长期驻留"语义完全靠 **checkpoint 持久化**而非显式状态机实现：从 checkpoint 角度，子图状态一直驻留在 SQLite，语义上"没退出"。用户多轮确认的复杂交互，对主图而言只是"同一线程的多次 invoke，中间被 interrupt 挂起"。

#### 2. Command 返回值约束驱动的工具写回模式：ToolRuntime 注入 + Command(update)

section 级 CRUD 工具需要同时读图状态（`current_draft`）和写回多个字段（`current_draft` + `last_draft` + `messages`）。LangGraph 1.2.6 的 `ToolNode` 对工具返回值有严格约束——只接受 `Command | ToolMessage | list`，裸 dict 直接抛 `TypeError`。

解决方案是用 `ToolRuntime` 作为工具参数（被 `ToolNode` 自动识别为注入参数，含 `state`/`tool_call_id`/`store` 等），工具内部读 `runtime.state`、返回 `Command(update={...})` 写回草稿字段并附带带真实 `tool_call_id` 的 `ToolMessage`。这是把"工具副作用写回图状态"这一需求，对齐到 LangGraph 的 `Command` 机制上，而非绕过 ToolNode 自己造轮子。

#### 3. plan 软约束 + 单步快照回退：用最小状态实现可控的人机协同循环

简历优化是"计划-执行-确认"的人机协同循环。设计中避免了两个重型方案：① 不用硬队列强制遵循 plan（`execute_router` pop 队首驱动 LLM），而是把 `plan_steps` + `steps_completed` 进度标记注入 system prompt 做软约束，LLM 自主按序执行，保留跳过不适用步骤的灵活性；② 不用版本栈做回退，而是每次 CRUD 前快照 `last_draft`，`step_confirm` 用户拒绝时恢复快照——单步回退用最小状态（一个 `last_draft` 字段）覆盖了"撤销上一步"的核心需求。

这是"**够用就好**"的工程取舍：plan 软约束承认 LLM 的动态判断价值，单步快照承认用户回退需求只关心"上一步"。两者都用最小状态字段实现，避免了过度设计。

#### 4. SSE CRLF 分帧 + 局部 interrupt 状态：前端流式调试的可观测性

自建 web 前端用 `fetch` + 手动 SSE 分帧（而非 `EventSource`，因 `EventSource` 不支持 POST）。踩了 sse-starlette 用 CRLF 行分隔的坑——`\r\n\r\n` 帧分隔与 LF 习惯不同。修复后前端能正确解析 token/interrupt/done 三类事件，把后端的 interrupt payload 渲染成带按钮的确认框。

配套的统一调试日志（`dlog` 带 `[scope:node]` 前缀 + 消息摘要）让"前端无反应"类问题能快速定位：后端日志确认 interrupt 已 yield → 前端调试日志显示 `总帧数=0` → 锁定 SSE 分帧问题。这是**端到端可观测性**的实践——前后端各有日志，问题定位不靠猜。

